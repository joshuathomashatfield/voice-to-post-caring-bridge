# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Data validation / settings
pydantic==2.9.2
pydantic-settings==2.5.2

# Speech-to-text
faster-whisper==1.0.3
numpy==1.26.4
soundfile==0.12.1

# Optional VAD (falls back to a built-in energy-based VAD if not installed)
webrtcvad==2.0.10

# LLM HTTP client (Ollama / OpenAI-compatible)
httpx==0.27.2

# Config
python-dotenv==1.0.1

# Optional server-side TTS (only needed if TTS_ENGINE=pyttsx3)
pyttsx3==2.90

# Testing
pytest==8.3.3
